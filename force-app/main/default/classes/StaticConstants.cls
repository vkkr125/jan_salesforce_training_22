public class StaticConstants {
    
    // use these variables to stop the triggers or control the triggers
    // important : how to break loop
    public static boolean runAccountTrigger = true;
    public static boolean tempAccountTrigger = true;
}
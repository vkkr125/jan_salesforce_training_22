public interface Flyer {
    
    // class will have concrete methods (methods with definition)
    // interface will have only function definition 
    // those classes will inherit it those have to implements all the below functions
    
    void takeoff();
    void fly();
    void land();
    
    
}